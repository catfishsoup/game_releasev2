

import { NavLink } from "react-router-dom";

const Logo = () => {
    return(
        <div >
            <NavLink to='/home' className='logo-name'>VIDEA</NavLink>
        </div>
    )
}

export default Logo